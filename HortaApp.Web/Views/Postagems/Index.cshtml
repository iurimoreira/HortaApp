@model IEnumerable<HortaApp.Domain.Postagem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Usuarioid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Conteudo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FotoPostagem)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Usuarioid)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Conteudo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FotoPostagem)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PostagemId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PostagemId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PostagemId })
        </td>
    </tr>
}

</table>
